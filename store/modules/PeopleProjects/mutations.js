export const SET_PEOPLE_PROJECT_DATA = (state , data ) => {
    state.PeopleProjectData = data;
}
export const SET_PEOPLE_PROJECTS_DATA = (state , data ) => {
    state.PeopleProjectsData = data;
}
export const SET_PEOPLE_PROJECTS_PAGINATION = (state , data ) => {
    state.PeopleProjectsPagination = data;
}

