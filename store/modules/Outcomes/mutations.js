export const SET_OUTCOME_DATA = (state , data ) => {
    state.OutcomeData = data;
}
export const SET_OUTCOMES_DATA = (state , data ) => {
    state.OutcomesData = data;
}
export const SET_OUTCOMES_PAGINATION = (state , data ) => {
    state.OutcomesPagination = data;
}

